{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build ROM",
            "type": "shell",
            "command": "mkdir build; rgbasm -Werror -Weverything game.asm -o build/game.o && rgblink --dmg --tiny build/game.o -o build/game.gb && rgbfix  --title MyGame --pad-value 0 --validate build/game.gb",
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Compiling the ROM",
        },
    ]
}