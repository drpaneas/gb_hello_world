{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build ROM",
            "type": "shell",
            "command": "mkdir build; rgbasm -L game.asm -o build/game.o && rgblink build/game.o -o build/game.gb && rgbfix -v -p 0xFF build/game.gb",
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Compiling the ROM",
        },
    ]
}